package struct

/**
  * Represents an RGB colour
  *
  * @param r red - `Int` from 0 to 255
  * @param g green - `Int` from 0 to 255
  * @param b blue - `Int` from 0 to 255
  */
final case class RGB(r: Int, g: Int, b: Int)
